# LitScope 知研：交互原型设计说明

这是测试题的提交稿，版本日期为 2026-08-25。

原型中的文献、作者、期刊、编号、检索来源与统计数字均为虚构的演示数据。

## 1. 方案概述

LitScope 不只是给论文打相关分。它先把科研人员的自然语言问题整理成可以检查的检索策略，再帮助用户判断哪些候选论文值得优先阅读，并说明判断依据和结果边界。用户确认本次检索有效后，还可以把它保存为关注课题，继续查看经过筛选的研究日报。

## 2. 目标用户与核心场景

目标用户是正在围绕明确研究问题开展综述、开题、论文写作或进展跟踪的科研人员。

用户真正需要完成的事情是：

当我只有一个自然语言研究问题时，我希望系统帮我找到真正值得优先阅读的论文，并说明检索范围和没有覆盖的内容。研究继续推进后，我也希望只收到与这个问题有关、值得关注的新进展，而不是一串未经筛选的订阅结果。

## 3. 核心流程

1. 输入自然语言研究问题；
2. 选择或配置数据库，查看连接状态；
3. 查看并调整时间、语言和文献类型等范围；
4. 查看系统拆出的概念组和同义词，必要时进行修改；
5. 获得候选文献和优先级；
6. 查看匹配证据、差异、局限和来源核验状态；
7. 将论文加入待读清单，并确认本次检索是否有效；
8. 保存为关注课题，继承问题、范围和筛选偏好；
9. 在研究日报中查看新增候选、去重结果和值得关注的进展；
10. 通过“有帮助”或“不相关”等反馈，调整后续筛选偏好。

建议演示顺序：

1. 在“智能检索”页直接点击“开始智能检索”；
2. 点击轻量流程导航中的“选择来源”，进入数据库与 API 中心；
3. 启停一个数据库，打开“配置”，填写 API Key 并执行演示连接测试；
4. 返回首页，演示“高级检索”或“导入检索式”，再开始检索；
5. 在结果页查看问题拆解、来源覆盖和前三篇候选；
6. 全选本页并演示批量加入待读，再导出 JSON / CSV / Markdown；
7. 点击第一篇标题或“查看依据”，打开判断依据侧栏；
8. 点击“保存为关注课题”，确认频率和最低推荐条件；
9. 进入“研究日报”，查看本次检索如何变成持续追踪；
10. 在结果页切换“无结果”或“来源异常”，演示非理想状态。

## 4. 关键产品判断与取舍

### 4.1 排序目标不是“最相关”，而是“最值得优先投入阅读时间”

单一相关分容易制造确定性错觉。因此结果卡同时展示：

- 研究对象、驱动因素、响应变量和方法的匹配证据；
- 与用户问题的差异和外推局限；
- 题录是否可核验、来自几个来源；
- 匹配度只作为快速导航，不替代解释。

排序文案明确说明：综合考虑问题匹配、证据完整度和来源可验证性。来源待核验的论文可以作为线索保留，但不获得“可信优先”加分。

### 4.2 先给摘要判断，再允许下钻证据

科研人员需要高效扫读，又不能牺牲可信性。结果卡只展示三条最关键理由；点击后再查看逐维度证据表、题录核验和外推限制，形成“快速判断—按需核查”的两层信息结构。

### 4.3 检索透明性放在结果同屏，而不是藏在设置页

范围和缺口会直接改变用户对结果的理解，所以结果页始终展示：

- 实际使用的来源及各自状态；
- 时间、语言和文献类型；
- 自然语言问题拆成了哪些概念组 / 同义词；
- 未连接来源和未覆盖内容。

这样用户可以判断“没搜到”是领域真的缺少研究，还是检索边界造成的。

### 4.4 只有在用户确认有效后，才把检索变成关注

主动检索负责建立研究意图和筛选边界；持续追踪不重新猜测用户需求，而是继承已经验证过的：

- 研究问题与扩展词；
- 来源、时间、语言和类型范围；
- 用户收藏 / 忽略行为；
- 最低推荐条件与来源核验偏好。

这让两类任务形成同一条连续工作流，而不是两个割裂功能。

### 4.5 日报的价值是“筛选后的变化”，不是“每天发一批论文”

日报首先说明新增候选数、降噪数和进入摘要数，再对每篇解释：

- 为什么与课题相关；
- 相比已看文献有什么增量；
- 哪些信息仍未核验；
- 为什么某篇只是方法补充而非最高优先级。

“有帮助 / 不相关”反馈只承诺调整后续排序与过滤，不宣称已经理解用户全部研究意图。

### 4.6 数据库与 API 是可管理的检索基础设施，不是后台黑盒

本次版本新增独立的“数据库与 API”中心：

- 预置 OpenAlex、Crossref、PubMed、arXiv、Semantic Scholar 和 Europe PMC 六种公开数据库适配器；
- 每个来源可启停，并显示学科覆盖、接口类型、鉴权要求和演示连接状态；
- 可设置 API Base URL、鉴权方式、请求频率和 API Key / Token；
- 支持添加自定义数据库与导入 JSON 配置；
- 配置保存在浏览器本地，密钥字段默认脱敏；
- 测试连接只模拟延迟与返回结构，不发送地址、密钥或网络请求。

这些公开数据库名称和接口地址用于展示未来扩展路径；论文结果仍全部为虚构的演示数据，符合测试题的数据边界。

### 4.7 操作按钮服务于真实任务，不作为视觉装饰

本版补充四类可操作能力：

- **高级检索**：编辑概念组、包含词、排除词并生成可复制布尔检索式；
- **导入检索式**：识别 PubMed、Web of Science、Scopus 或通用 Boolean 格式并回填研究问题；
- **批量处理**：全选 / 取消、批量加入待读、创建阅读分组；
- **结果导出**：在浏览器本地生成 JSON、CSV 或 Markdown 检索报告。

原首页的三张产品说明卡已移除，替换为一行四步的可操作流程导航，依次为研究问题、选择来源、智能检索和阅读决策。

## 5. 主动检索与持续追踪如何衔接

| 主动检索阶段形成的资产 | 保存关注时的处理 | 日报中的用途 |
|---|---|---|
| 自然语言研究问题 | 保存为关注课题的目标描述 | 判断新论文是否仍回答同一问题 |
| 概念组、同义词、排除词 | 允许用户确认后继承 | 用于增量检索和降噪 |
| 时间、语言、类型、来源 | 明确展示并继承 | 每期继续声明覆盖边界 |
| 收藏、忽略和查看依据行为 | 转成初始偏好信号 | 调整新文献优先级 |
| 最低匹配与核验要求 | 在保存弹窗再次确认 | 决定进入日报或仅作为待核验线索 |

原型用“来源：一次有效检索”和右侧时间线，显式解释这一衔接关系。

## 6. 可信性与检索边界处理

### 6.1 题录真实性

- 标题、作者、摘要、编号和来源分别记录，不把“模型生成内容”当成题录事实；
- 通过多个来源字段一致性展示“已核验 / 单一来源 / 暂不可验证”；
- 来源异常时保留线索、降低可信优先级并提示用户，不把无法验证等同于虚假；
- 本次所有文献均明确标为演示数据，不提供可能被误认为真实论文的外链。

### 6.2 推荐可解释性

- 推荐理由落到可读的文献字段或摘要证据，而不是只显示“高度相关”；
- 区分“关联”“差异”“局限”，避免把研究对象相似直接解释为结论可外推；
- 匹配度用于排序，不伪装成精确的科学概率。

### 6.3 检索范围透明性

- 展示完成、部分可用、未连接的来源；
- 展示实际检索词、时间、语言和文献类型；
- 明确说明付费库全文、非中英文资料和范围外年份未覆盖；
- 无结果页面明确写出“不等于没有相关研究”，并让用户有控制地放宽范围。

## 7. 非理想状态

原型提供三个可切换状态：

1. **无结果**：说明当前严格条件与来源限制，建议扩大年份、概念范围或查看可迁移的方法证据；
2. **来源异常**：把来源状态改为“部分 / 异常”，保留高度相关线索但降低可信排序，并在卡片上标记待核验；
3. **用户修改偏好**：日报反馈会改变后续推荐偏好，给出即时确认，但不声称已执行真实推送。

## 8. 当前版本的信息架构

- 智能检索：问题输入、范围、产品机制预期；
- 检索结果：可解释排序、判断依据、检索边界、非理想状态；
- 研究日报：新增进展、推荐增量、可信状态、反馈；
- 数据库与 API：来源启停、地址、鉴权、密钥、本地保存、演示连接测试和自定义适配器；
- 待读清单 / 关注课题：提供轻量弹窗承接批量操作和日报入口，不展开完整管理功能。

## 9. 成功指标（若进入真实验证）

当前版本优先验证“是否改善阅读决策”，暂不追求搜索量：

- **首要指标：优先阅读决策有效率**——用户标为值得读的 Top 10 结果占比；
- 决策效率：从提交问题到选出首批 3 篇待读文献的中位时长；
- 可解释性：用户看完推荐依据后能否复述至少一条匹配理由和一条局限；
- 边界理解：用户是否知道至少一个本次未覆盖的来源 / 范围；
- 日报质量：进入日报的论文中，被标为“有帮助”的比例；
- 降噪效果：与普通关键词订阅相比，无关条目减少比例；
- 护栏指标：来源待核验内容被用户误认为已核验的比例。

## 10. 本次未覆盖

- 真实数据库请求、全文获取、真实跨库去重与检索算法；
- 匹配分数的模型训练、标定及离线 / 在线评测；
- 账号、团队协作、权限、付费与版权处理；
- 真实定时任务、邮件 / App 推送和消息触达；
- 完整待读清单、批注、引用管理和写作工作流；
- 无障碍与移动端的完整设计体系（原型仅做基础响应式适配）；
- 真实数据下的隐私、机构订阅授权和合规审计。

这些内容并非不重要，而是本题优先验证“从问题到可信阅读决策，再到持续追踪”的核心闭环。

## 11. 原型实现说明

- 载体：单文件 HTML（内嵌 CSS / JavaScript，无第三方依赖）；
- 运行：双击 `1_可点击原型_LitScope.html`；
- 推荐浏览器：最新版 Chrome 或 Edge，建议窗口宽度不低于 1280 px；
- 数据：与 `3_Mock文献数据.json` 对应的虚构示例；
- API 配置与启停状态使用浏览器 `localStorage` 保存，无法使用时会安全降级；
- JSON / CSV / Markdown 导出文件由浏览器本地生成；
- 所有数据库连接测试均为前端演示交互，不会联网、提交密钥或创建真实推送。

